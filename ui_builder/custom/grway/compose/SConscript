Import('AIC_ROOT')
Import('PRJ_KERNEL')
from building import *

cwd = GetCurrentDir()
CPPPATH = []
src = []

if GetDepend('GW_COMPOSE'):
    src = Glob('src/*.c')
    CPPPATH = [cwd]

group = DefineGroup('gw-compose', src, depend = [''], CPPPATH = CPPPATH)
Return('group')