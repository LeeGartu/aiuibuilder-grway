Import('AIC_ROOT')
Import('PRJ_KERNEL')
from building import *

cwd = GetCurrentDir()
CPPPATH = []
src = []

if GetDepend('GW_CONFIG'):
    src = Glob('src/*.c')
    CPPPATH = [cwd]

group = DefineGroup('gw-config', src, depend = [''], CPPPATH = CPPPATH)
Return('group')