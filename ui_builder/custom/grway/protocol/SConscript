Import('AIC_ROOT')
Import('PRJ_KERNEL')
from building import *

cwd = GetCurrentDir()
CPPPATH = []
src = []

if GetDepend('GW_PROTOCOL'):
    src = Glob('src/*.c')
    CPPPATH = [cwd]

group = DefineGroup('gw-protocol', src, depend = [''], CPPPATH = CPPPATH)
Return('group')